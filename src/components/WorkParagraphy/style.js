import styled from 'styled-components';

export const ParagraphyContainer = styled.p`
    color: var(--color-slate-gray);
    font-size: 1.7rem;
    font-weight: 400;
    font-family: var(--font-sans);

    svg {
        font-size: 1rem;
    }
`;
